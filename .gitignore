*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
app/build/
*.apk
*.aab
*.keystore
!debug.keystore

# The real Firebase config must not be committed.
# A placeholder google-services.json is tracked so the project builds;
# replace it locally with your own from the Firebase console.
